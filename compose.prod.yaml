services:
  frontend:
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
    restart: always

  backend:
    ports:
      - 8080:8080
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always
